select *
from employee_info;

select first_name, 
last_name, 
birth_date,
age,
age + 10
from employee_info;
# PEMDAS, mysql follows to do any kind of calculations

select distinct gender
from employee_info;
# Selects whats unique in the column, like categorizes it

